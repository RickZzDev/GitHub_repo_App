import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:get/get.dart';

const textStyle = TextStyle(fontSize: 18, color: Colors.white);

var secondaryColor = Colors.blueAccent[400];

var primaryColor = Colors.blueAccent;

var screenHeigthSize = MediaQuery.of(Get.context).size.height;

var screenWidthhSize = MediaQuery.of(Get.context).size.width;
